exec pint parse 'bar anchored'
cmp stdout stdout.txt
! stderr .

-- stdout.txt --
++ node: bar anchored
  VectorSelector:
    * Type: vector
    * Name: bar
    * Offset: 0s
    * LabelMatchers: [__name__="bar"]
    * Anchored: true
